# Global Proxy Settings
proxy_set_header Host $host;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;

# Homarr Dashboard (Default)
server {
    listen 80;
    server_name dashboard.local "";

    location / {
        proxy_pass http://homarr:7575;
    }
}

# Network Services
server {
    listen 80;
    server_name pihole.local;

    location / {
        proxy_pass http://pihole;
    }
}

# # Media Services
# server {
#     listen 80;
#     server_name torrent.local;
#
#     location / {
#         proxy_pass http://torrent:8080;
#     }
# }
#
# server {
#     listen 80;
#     server_name jackett.local;
#
#     location / {
#         proxy_pass http://jackett:9117;
#     }
# }
#
# server {
#     listen 80;
#     server_name legendas.local;
#
#     location / {
#         proxy_pass http://bazarr:6767;
#     }
# }
#
# server {
#     listen 80;
#     server_name indexas.local;
#
#     location / {
#         proxy_pass http://prowlarr:9696;
#     }
# }
#
# server {
#     listen 80;
#     server_name filmes.local;
#
#     location / {
#         proxy_pass http://radarr:7878;
#     }
# }
#
# server {
#     listen 80;
#     server_name series.local;
#
#     location / {
#         proxy_pass http://sonarr:8989;
#     }
# }

# Utility Services
server {
    listen 80;
    server_name speedtest.local;

    location / {
        proxy_pass http://speedtest;
    }
}

server {
    listen 80;
    server_name speedtest-tracker.local;

    location / {
        proxy_pass http://speedtest-tracker;
    }
}

# OctoPrint - Configuração Otimizada
server {
    listen 80;
    server_name octoprint.local;

    # 3. Permite uploads de arquivos GCODE pesados sem erro 413 (Payload Too Large)
    client_max_body_size 0;

    location / {
        proxy_pass http://octoprint;

        # 1 & 2. Suporte obrigatório a WebSockets para comunicação em tempo real da interface
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 4. Header necessário para o OctoPrint validar CSRF e resolver rotas internas
        proxy_set_header X-Scheme $scheme;
        
        # Garante que o host original seja passado para o backend
        proxy_set_header Host $host;

        # Opcional: Aumenta o timeout para evitar desconexões em operações longas
        proxy_read_timeout 120s;
    }

    # 5. Bloco dedicado para o stream da webcam
    location /webcam/ {
        proxy_pass http://octoprint/webcam/;
        
        # Desabilita o buffering para garantir que o vídeo seja transmitido sem latência
        proxy_buffering off;
    }
}
